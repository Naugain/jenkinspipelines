pipeline {
    agent any
        stages {
            stage ('First test') {
                steps{
                    sh 'echo "starting the pipeline"'
                    sh 'mkdir mrinalfolder || echo "file exists"'
                    echo 'test 5 worked'
                    
                }
            }
        }
}
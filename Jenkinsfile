/* change to check git fetch & merge */
pipeline {
    agent any
        stages {
            stage ('First test') {
                steps{
                   sh 'echo "starting the pipeline"'
                   sh 'mkdir mrinalfolder'
                   echo 'test 5 worked'
                }
            }
        }
}
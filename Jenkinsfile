pipeline {
    agent any
        Stages {
            stage{
                steps{
                    sh 'echo "starting the pipeline'
                    sh 'mkdir mrinalfolder'
                    echo 'test 2 worked'
                }
            }
        }
}
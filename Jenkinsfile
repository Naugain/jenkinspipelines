pipeline {
    agent any
        Stages ('test1') {
            stage{
                steps{
                    sh 'echo "starting the pipeline"'
                    sh 'mkdir mrinalfolder'
                    echo 'test 2 worked'
                    
                }
            }
        }
}